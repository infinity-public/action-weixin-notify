name: 测试

on:
  pull_request:
    types:
      - closed
    branches:
      - main
      - beta

jobs:
  Notify:
    if: github.event.pull_request.merged == true  # 确保是合并
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Send Notify
        uses: ./
        with:
          notify-url: ${{ secrets.NOTIFY_URL }}
          notify-project: '测试 Notify'
          deploy-success: 'false'
